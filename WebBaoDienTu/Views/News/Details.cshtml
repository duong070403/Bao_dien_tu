@model WebBaoDienTu.Models.News

@{
    ViewData["Title"] = Model.Title;
    Layout = "_Layout";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-12 text-center">
            <h1 class="fw-bold">@Model.Title</h1>
            <p class="text-muted">🕒 @Model.CreatedAt.ToString("dd/MM/yyyy HH:mm") | 🖊️ Tác giả: @Model.Author.FullName</p>
        </div>
    </div>

    <div class="row">
        <div class="col-md-10 offset-md-1">
            @* Chỉ hiển thị ảnh từ ImageUrl nếu ảnh không có trong Content *@
            @if (!string.IsNullOrEmpty(Model.ImageUrl) && !Model.Content.Contains(Model.ImageUrl))
            {
                <div class="text-center mb-3">
                    <img src="@Model.ImageUrl" alt="Ảnh minh họa" class="img-fluid rounded shadow-sm" style="max-height: 400px; object-fit: cover;">
                </div>
            }

            <div class="content">
                @Html.Raw(Model.Content)
            </div>

            <div class="d-flex justify-content-between align-items-center mt-4">
                <span class="badge @(Model.IsApproved ? "bg-success" : "bg-warning text-dark")">
                    @(Model.IsApproved ? "✅ Đã duyệt" : "⏳ Chờ duyệt")
                </span>
                <span class="text-muted">📌 Danh mục: @Model.Category.CategoryName</span>
            </div>

            <hr />

            <div class="text-center mt-4">
                <a asp-action="Edit" asp-route-id="@Model.NewsId" class="btn btn-warning">
                    <i class="fas fa-edit"></i> Sửa bài viết
                </a>
                <a asp-action="Index" class="btn btn-secondary ms-2">
                    <i class="fas fa-arrow-left"></i> Quay lại danh sách
                </a>
            </div>
        </div>
    </div>
</div>